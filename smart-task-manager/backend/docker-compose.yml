version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    depends_on: []

  # Optional Postgres service - uncomment and update application.yml to use it
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: smarttask
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
